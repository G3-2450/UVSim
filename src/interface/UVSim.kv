#:kivy 1.0.9


# GENERAL LAYOUT
# BoxLayout:
    # Left pane

    # BoxLayout (Right pane)
        # BoxLayout ( registers and memory)
        # BoxLayout ( console )
    

<UVSimWindow>:
    orientation: 'horizontal' # Two panes side-by-side
    spacing: dp(22) # Space between panes
    padding: dp(22) # Padding around window content
    size_hint_x: 1
    size_hint_y: 1

    canvas.before:
        Color:
            rgba: 0.0, 0.3, 0.15, 1
        Rectangle:
            pos: self.pos
            size: self.size

    # LEFT PANE
    BoxLayout:
        size_hint_x: 2/5
        size_hint_y: 1
        orientation: 'vertical'
        LeftPaneWidget:
            id: left_panel
        
    # RIGHT PANE
    BoxLayout:
        id: right_pane
        orientation: 'horizontal'
        size_hint_x: 3/5
        size_hint_y: 1
        spacing: dp(22) # Space between Console and Reg/Mem sections
        padding: dp(22)
        
        canvas.before:
            Color:
                rgba: .1608, .1608, .1608, 1 # Background color
            RoundedRectangle:
                pos: self.pos
                size: self.size
                radius: [dp(10)]
        
        # REGISTERS AND MEMORY
        MemRegWidget:
            id: mem_reg_display
            size_hint_x: 2/5
            size_hint_y: 1

        # CONSOLE SECTION
        ConsoleWidget:
            id: uvsim_console
            size_hint_x: 3/5
            size_hint_y: 1
